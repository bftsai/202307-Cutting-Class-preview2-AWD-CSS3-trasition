$(document).ready(function () {
    /*
    $('.sheet').click(function (e) { 
        e.preventDefault();
        $('.compare').toggleClass('active');
    });
    $('.button-message').click(function (e) { 
        e.preventDefault();
        
        $('.button-textarea').toggleClass('button-textarea-active');
        
       
       $('.button-textarea').slideToggle(100000);
       
    });
    $('.button-profile').click(function (e) { 
        e.preventDefault();
        $('.button-enter-profile').toggleClass('button-enter-profile-active');
    });
    */
});